package com.example.core.config

/**
 * GeofenceConfig holds configuration constants for geofencing features.
 * it can not be chnaged at runtime.
 */

object GeofenceConfig {
    const val DEFAULT_RADIUS_METERS = 200f
}